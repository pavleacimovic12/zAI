# Streamlit Secrets Configuration
# This file prevents secrets access errors during deployment
# Add your OpenAI API key here for enhanced responses (optional)

# OPENAI_API_KEY = "your-api-key-here"

# Note: On Posit Connect, you can set environment variables instead
# The app works perfectly without this API key using comprehensive fallback responses